parameters:
  base-dir:
    default: "{cwd}"
  target:
    default: "output/spec"
emit:
  - "@typespec/json-schema"
options:
  "@typespec/json-schema":
    emitter-output-dir: "{base-dir}/output"
    bundleId: "deliveryddinfo.schema.json"
    emitAllModels: true      # no @jsonSchema needed
    file-type: "json"
    emitAllRefs: true 

